# =================================================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO - UDLA STATS
# =================================================================
# Este archivo contiene todas las variables necesarias para ejecutar
# el stack completo de servicios con Docker Compose
#
# INSTRUCCIONES:
# 1. Copia este archivo: Copy-Item .env.example .env
# 2. Edita el archivo .env y cambia los valores de ejemplo
# 3. NUNCA subas el archivo .env al repositorio
# =================================================================

# =================================================================
# VARIABLES DE POSTGRESQL (REQUERIDAS)
# =================================================================

# Usuario administrador de PostgreSQL
POSTGRES_USER=postgres

# Contraseña del usuario administrador
# ⚠️  CAMBIAR POR UNA CONTRASEÑA SEGURA
# Recomendación: mínimo 12 caracteres, mayúsculas, minúsculas, números y símbolos
POSTGRES_PASSWORD=CambiaEstaClaveSegura123!

# Nombre de la base de datos principal
POSTGRES_DB=udla_stats

# =================================================================
# NOTAS DE SEGURIDAD
# =================================================================
# ⚠️  IMPORTANTE:
# - NUNCA subas el archivo .env con contraseñas reales al repositorio
# - Usa contraseñas robustas: mínimo 12 caracteres
# - Incluye mayúsculas, minúsculas, números y símbolos especiales
# - Rota las contraseñas periódicamente
# - Evita palabras del diccionario o información personal
# - Ejemplo de contraseña segura: Pg#9kL$mN2qR@vX8
#
# 🔒 PARA PRODUCCIÓN:
# - Considera usar Docker Secrets en lugar de variables de entorno
# - Configura un firewall para restringir acceso a puertos
# - Cambia DEBUG=false
# - Restringe ALLOWED_HOSTS a dominios específicos
#
# =================================================================
# COMANDOS ÚTILES
# =================================================================
# 
# Iniciar todos los servicios:
# docker compose up -d
#
# Ver estado de servicios:
# docker compose ps
#
# Ver logs:
# docker compose logs -f
# docker compose logs -f postgres
# docker compose logs -f authservice
#
# Detener servicios:
# docker compose stop
#
# Eliminar contenedores (mantiene datos):
# docker compose down
#
# Eliminar contenedores y volúmenes (BORRA DATOS):
# docker compose down -v
#
# Conectar a PostgreSQL:
# docker compose exec postgres psql -U postgres -d udla_stats
#
# Acceder al shell de un contenedor:
# docker compose exec postgres bash
# docker compose exec authservice bash

# =================================================================
# VARIABLES DE AUTHSERVICE - DJANGO (REQUERIDAS)
# =================================================================

# Superusuario de Django - Credenciales de acceso al admin
# Usuario administrador que se creará automáticamente
DJANGO_SUPERUSER_USERNAME=admin

# Contraseña del superusuario de Django
# ⚠️  CAMBIAR POR UNA CONTRASEÑA SEGURA
DJANGO_SUPERUSER_PASSWORD=AdminPassword123!

# Email del superusuario (usado para recuperación de cuenta)
DJANGO_SUPERUSER_EMAIL=admin@udla.edu.ec

# =================================================================
# CONFIGURACIÓN GENERAL DE AUTHSERVICE
# =================================================================

# Modo de depuración (true para desarrollo, false para producción)
# ⚠️  Cambiar a false en producción
DEBUG=true

# Hosts permitidos para acceder a la API (separados por comas)
# Ejemplos: localhost,127.0.0.1,tudominio.com
ALLOWED_HOSTS=localhost,127.0.0.1

# URL del frontend (para configuración de CORS)
# Cambiar según tu configuración de frontend
FRONTEND_URL=http://localhost:4321

# Puerto en el que se ejecutará la API de AuthService
# Este puerto debe coincidir con el puerto expuesto en docker-compose.yml
API_PORT=8010
